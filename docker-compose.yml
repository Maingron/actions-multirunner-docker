# AUTO-GENERATED from config.yml by render.sh. DO NOT EDIT.
# Re-run `./render.sh` (or just `./start.sh`) after changing the runner inventory.

x-runner-base: &runner-base
  restart: unless-stopped
  ulimits:
    nofile: 1048576
    nproc: 1048576
  pids_limit: -1
  volumes:
    - ./config.yml:/etc/github-runners/config.yml:ro
    - runner-state:/var/lib/github-runners
  tmpfs:
    - /home/github-runner:exec,size=8g,mode=0755,uid=1000,gid=1000

services:
  # image: debian:stable-slim
  runners-debian-stable-slim:
    <<: *runner-base
    build:
      context: .
      args:
        RUNNER_VERSION: "2.334.0"
        BASE_IMAGE: debian:stable-slim
        RUNNER_IMAGE_FLAVOR: debian:stable-slim
    image: github-multirunner:debian-stable-slim
    environment:
      GITHUB_PAT: ${GITHUB_PAT:-}
      RUNNER_IMAGE_FLAVOR: debian:stable-slim

  # image: ubuntu:24.04
  runners-ubuntu-24-04:
    <<: *runner-base
    build:
      context: .
      args:
        RUNNER_VERSION: "2.334.0"
        BASE_IMAGE: ubuntu:24.04
        RUNNER_IMAGE_FLAVOR: ubuntu:24.04
    image: github-multirunner:ubuntu-24-04
    environment:
      GITHUB_PAT: ${GITHUB_PAT:-}
      RUNNER_IMAGE_FLAVOR: ubuntu:24.04

volumes:
  runner-state:
